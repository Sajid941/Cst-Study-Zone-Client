import { Helmet } from "react-helmet";

const Home = () => {
    return (
        <div>
        <Helmet>
            <title>Home | Cst Study Zone</title>
        </Helmet>
        </div>
    );
};

export default Home;