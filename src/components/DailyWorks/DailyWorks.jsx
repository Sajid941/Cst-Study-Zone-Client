import SectTitle from "../SectTitle/SectTitle";

const DailyWorks = () => {
    return (
        <section>
            <SectTitle  title="Daily Works" />
        </section>
    );
};

export default DailyWorks;